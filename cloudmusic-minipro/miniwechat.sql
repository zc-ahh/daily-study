create database miniwechat;
use database;
create table user(
user varchar(20) primary not null,
name varchar(20),
passwoed varchar(20) not null,
grade varchar(20),
classs varchar(20),
major varchar(20),
sex varchar(20)
);